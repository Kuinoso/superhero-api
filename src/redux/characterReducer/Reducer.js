import * as actions from './Constants.js';

const initialState = {
    specificData: null,
  };
  
  export default function Reducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
  }